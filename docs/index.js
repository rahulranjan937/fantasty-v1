import { openapi, info, servers } from "./info.js";
import { components, paths, security, tags } from "./blogs.js";

const docs = {
  openapi,
  info,
  servers,
  paths,
  components,
  security,
  tags,
};

export default docs;
